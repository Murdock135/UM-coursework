function [y_next] = Euler(f,x0,y0,x_end)
%Euler(f,t0,y0,b1,b2) integrates an ODE, f from b1 to b2 using initial
%conditions t0 (independent var) and y0 (dependent var)

i = 1; %iteration
y(i) = y0; %initial condition
x(i) = x0; %initial condition

x_end = b1
y(i+1) = y(i)+h*f(x(i),y(i));

